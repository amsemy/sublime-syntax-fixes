sublime-syntax-fixes
====================

Syntax Fixes for Sublime Editor

####Shell Script (Bash)

```bash
echo ${FOO:+default}
echo ${FOO:-"default"}
echo ${FOO:-${BAR}}
echo ${FOO:+"${BAR:+"${BAZ}"}"}
```
